export interface GoogleResponse {
  email: string;
  firstName: string;
  lastName: string;
  displayName: string;
  picture: string;
  verified: boolean;
  accessToken: string;
  refreshToken: string;
}
